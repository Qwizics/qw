import importerSelectors from 'src/modules/shared/importer/importerSelectors';

const examAnswerImporterSelectors = importerSelectors(
  'examAnswer.importer',
);

export default examAnswerImporterSelectors;
