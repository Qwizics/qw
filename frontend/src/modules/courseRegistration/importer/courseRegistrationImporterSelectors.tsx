import importerSelectors from 'src/modules/shared/importer/importerSelectors';

const courseRegistrationImporterSelectors = importerSelectors(
  'courseRegistration.importer',
);

export default courseRegistrationImporterSelectors;
